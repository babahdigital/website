{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Auto Commit and Push",
            "type": "shell",
            "command": "powershell",
            "args": [
                "-Command",
                // Tambahkan pull rebase sebelum add, commit, dan push:
                "git pull --rebase origin main; git add .; git commit -m \"Auto-commit: $(Get-Date -Format yyyy-MM-dd_HH:mm:ss)\"; git push origin main"
            ],
            "problemMatcher": [],
            "presentation": {
                "reveal": "never"
            },
            "options": {
                "env": {
                    "TASK_COMPLETED_MESSAGE": "Git Auto Commit and Push Successful!"
                }
            }
        }
    ]
}